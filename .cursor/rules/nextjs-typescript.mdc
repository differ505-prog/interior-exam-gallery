---
description: Next.js 15 App Router + TypeScript + Tailwind 開發標準
globs:
  - "src/app/**/*.ts"
  - "src/app/**/*.tsx"
  - "src/components/**/*.ts"
  - "src/components/**/*.tsx"
  - "src/lib/**/*.ts"
  - "src/types/**/*.ts"
alwaysApply: false
---

# Next.js 15 App Router + TypeScript + Tailwind 開發標準

本規則適用於 `src/` 目錄下所有 TypeScript / TSX 檔案。

## 1. App Router 路由規範

- **Server Components 優先**：所有 `page.tsx` 和非互動性元件預設為 Server Component
- **Client 元件判定**：有 `useState`、`useEffect`、事件處理、`"use client"` 宣告者才使用 Client Component
- **API Routes**：`src/app/api/` 下的 `route.ts` 處理所有 Supabase server-side 請求，`SUPABASE_SERVICE_ROLE_KEY` 嚴禁離開 server
- **Metadata**：每個 `page.tsx` 必須匯出 `metadata` 物件（Title、Description、Open Graph）

## 2. TypeScript 型別規範

**嚴格模式**：不得使用 `any`，全部使用明確型別或 `unknown` + 斷言。

主要型別定義於 `src/types/exam.ts`：

```typescript
// ArchiveItem — 單張試卷
interface ArchiveItem {
  code: string;           // 例如 "201-A"
  title: string;           // 例如 "平面圖 201"
  section: string;         // "plan" | "ceiling-elevation" | "perspective" | "detail"
  variants?: string[];     // 版本陣列
  focus: string;           // 考試重點（鎖定）
  notes: string[];          // 扣分點（鎖定）
  imageUrl?: string;       // Supabase Storage URL（可選）
}

// UploadEntry — 上傳練習記錄
interface UploadEntry {
  id: string;
  userId?: string;
  section: string;
  code: string;
  imageUrl: string;
  selfReview?: string;     // 自評缺點
  createdAt: string;
}
```

**嚴禁**：在 `src/data/exam-content.ts` 以外的位置定義重複的試卷資料。

## 3. Tailwind CSS 使用規範

- **JIT 模式**：所有 class 皆以实际使用的為準，不得預先聲明未使用的 utilities
- **品牌色使用**：暖棕 `#876f49` 使用 `bg-[#876f49]`、`text-[#876f49]`、`bg-[#876f49]/10`（淺底），避免濫用
- **自訂 CSS 變數**：所有自訂色彩統一在 `src/app/globals.css` 的 `:root` 宣告，不得在 inline style 寫死 hex
- **響應式前綴**：標準斷點 `md:`（768px）、`lg:`（1024px）、`xl:`（1200px）

## 4. 元件結構紀律

**資料流向**：
```
src/data/exam-content.ts (內容鎖定)
    → src/components/archive-section.tsx (Server Component)
    → src/components/archive-section-client.tsx (Client Component)
    → src/components/archive-card.tsx (純展示)
    → src/components/archive-detail-modal.tsx (Lightbox + 自評)
```

**禁止**：
- 在 Server Component 使用 `"use client"` 以外的 client-side API
- 在 Client Component 直接 import server-only 模組（如 `src/lib/supabase.ts` 的 server client）
- 在 `src/lib/supabase.ts` 外創建新的 Supabase client 實例

**允許**：
- `src/lib/supabase.ts` 匯出兩個 client：`createBrowserClient()`（前端）和 `createServerClient()`（後端）
- API Route 直接使用 `SUPABASE_SERVICE_ROLE_KEY`

## 5. 圖片處理規範

所有 `<img>` 必須：
1. 有 `alt` 屬性（有意義描述，不可為空字串，除非裝飾性圖片則 `alt=""`）
2. 有 `width` 和 `height` 屬性（與 `aspect-ratio` 一致，防止 CLS）
3. 有 `onError` Fallback（使用 `safe-image.tsx` 元件自動處理）

優先使用 `src/components/ui/safe-image.tsx`，避免手寫 `<img>`。

## 6. 表單與上傳

上傳功能使用 `src/app/api/uploads/route.ts`（POST）：
- 支援 `multipart/form-data`
- 存儲至 Supabase Storage `practice-images` bucket
- 返回 public URL
- 前端使用 `src/components/upload-studio.tsx`

## 7. 效能優化

- `next/image`：本專案不使用（使用 Supabase Storage URL），改用自訂 `safe-image.tsx`
- 動態 Import：Large Modal (`archive-detail-modal.tsx`) 建議使用 `next/dynamic` 延遲載入
- 字體：`next/font/google` 預設已啟用（`globals.css` 設定 `--font-sans`）

## 8. 環境變數管理

不得在前端程式碼中使用 `process.env.NEXT_PUBLIC_*` 以外的環境變數。
`SUPABASE_SERVICE_ROLE_KEY` 僅允許出現在 `route.ts`（API Routes）和 `src/lib/supabase.ts` 的 server client 中。
