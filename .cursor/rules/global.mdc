---
description: Global system constitution — applies to all files in this project
globs:
  - "**/*"
alwaysApply: true
---

# 系統憲法 — Draft Gallery 乙級術科

本規則為本專案全域適用的系統憲法。完整版本（含 8 條 SOP、豁免清單、衝突解決協議）請見專案根目錄 `AGENTS.md`。

## 設計與美學原則（詳見 AGENTS.md 第一章）

**原則 1–4**：Stripe 資訊邏輯、Apple 極簡視覺、色彩紀律鎖定、高階微互動。

品牌色：`#876f49`（暖棕），全域透過 CSS 變數覆用，嚴禁每次生成不同色碼。

## 工程與資安原則（詳見 AGENTS.md 第二章）

**原則 5–12**：DRY 與模組化、語意與 SEO、防禦性 UI、資安防禦、內容鎖定、修改紀律、視覺驗收、容器寬度紀律。

### §12 容器寬度紀律（Container Width Discipline）

**條文**：所有「文字段落」、「卡片描述」、「區塊說明文字」一律禁止使用 `max-width: NNch` 的窄寬斷尾（如 `max-width: 60ch`、`max-width: 70ch`）。

**唯一例外**：
- Hero 主視覺的主標題（H1），可用 `max-width` 控制排版韻律。
- 長篇引言區（如聖經經文、設計宣言），須包在 `.container-tight` 內並明確標示為「引言」。

**理由**：窄寬斷尾會在桌機寬螢幕下產生「中央一條窄欄 + 兩側大片空白」的破碎佈局，破壞 Stripe 骨架的 Grid 系統。

**RWD 紀律**：
- 任何 Tab / Pill 按鈕容器，若標籤文字 ≥ 6 字元，必須使用 `overflow-x: auto` + `flex-wrap: nowrap`，禁止 `flex-wrap: wrap` 導致按鈕高度炸裂。
- Tab 標籤收合為「僅圖示」模式的 RWD 斷點統一為 **1024px**（桌機/平板分界），不用 768px。
- 統計卡網格在 `≤ 480px` 時改為 `2 × 2` 雙欄，禁止單欄堆疊造成過度捲動。

### §12-A 字距與視覺權重紀律（Typography & Visual Weight）

**條文 1 — 標題字距對齊**：
- H1 / H2 標題內若混用「中文 + 數字 + 半形括弧」，禁止對個別文字套用 `letter-spacing`，否則會產生奇怪空隙（例如「3小時」與「動態」之間斷裂）。
- 標題統一用 `text-wrap: balance` + 容器級 `gap`，不對個別字元加字距。

**條文 2 — 統計卡視覺權重均衡**：
- 多張同性質的「指標卡 / 統計卡」視覺權重必須一致。
- 唯一可標記為「highlight」的卡片：當且僅當其代表「**核心成就**」或「**當前焦點**」，且必須用 `aria-current="true"` 標註語意。
- highlight 卡的色彩必須仍與背景維持低對比（ΔE ≤ 8），禁止用全飽和 `var(--accent)`。

**條文 3 — 卡片標題行高一致性**：
- 多張並排的卡片（≥ 3 張），若標題長度不一導致換行數不同，必須設定 `min-height` 鎖定至少 2 行的容器高度，避免棋盤式錯位。
- 強制使用 `display: flex; flex-direction: column` + 標題區塊固定高度，內容區塊彈性伸展。

試卷題庫定義於 `src/data/exam-content.ts`，共 180 張試卷，`code` / `focus` / `notes` 欄位為內容鎖定白名單，嚴禁擅自增刪修改。

## Pre-flight Checklist（每次生成前必跑）

見 `AGENTS.md` 第五章。至少完成以下檢查後再生成新程式碼：
1. `grep -rn "var(--" src/` — 確認 CSS 變數覆用
2. `grep -rn "font-size:\|letter-spacing:" src/` — 確認字體合規
3. `grep -rEho "@media \(max-width: [0-9]+px\)" src/` — 確認不新增非標準斷點
4. `ls src/components/` — 確認無重複元件
5. 確認新增色彩集中於 `src/app/globals.css` 的 `:root` 區塊
6. 確認 font-size ≤ 6rem
7. 確認 ≥3rem 標題有 `text-wrap: balance`
8. 確認 commit 前本地 `npm run dev` 可正常運行

## 衝突解決（詳見 AGENTS.md 第四章）

三級分級：
- **第一級（自動修）**：`≥ 0.06em` 字距、超過 6rem 字級、缺 `alt`、寫死 hex
- **第二級（必須詢問）**：SOP 閾值邊緣 ±10% 的邊界值
- **第三級（必須修憲才能動）**：與設計哲學層級衝突

## 品牌詞庫（詳見 AGENTS.md 附錄 C）

固定詞組：「乙級術科」「Draft Gallery」「平面圖 201–206」「透視圖 207–212」「大樣圖 213–224」「練習圖上傳」「扣分點分析」「自評複盤」。不得擅自改寫。
