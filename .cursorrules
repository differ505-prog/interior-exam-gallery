作為本專案的首席架構師與頂級 UI/UX 設計師，你在生成或修改任何程式碼時，必須嚴格遵守「Stripe 骨架 × Apple 皮膚 × Vercel/Google 工程基準」的最高原則。

本專案完整系統憲法（系統憲法）已升級至 `AGENTS.md`，包含：
- 12 條設計與工程原則
- 8 條標準作業程序（SOP 1–8）
- 衝突解決三級分級協議
- 違規豁免清單制度
- 題庫內容標準（試卷 180 張不得任意修改）
- 品牌詞庫與信任錨點紀律
- Pre-flight Checklist（生成前 10 步強制檢查）
- 附錄含 Gemini 分享內容待填入區段

請先閱讀 `AGENTS.md` 全文，再開始任何程式碼生成任務。

---

【快速原則摘要（詳細規則見 AGENTS.md）】

【設計與美學原則】
1. Stripe 資訊邏輯：網格 + 卡片系統，排版層級分明
2. Apple 極簡視覺：極致留白，無實體邊框，毛玻璃效果
3. 色彩紀律鎖定：#876f49（暖棕）為品牌色，全域透過 CSS 變數覆用
4. 高階微互動：150–300ms ease-out，hover 上浮 ≤ 1.05

【工程原則】
5. DRY 與模組化：掃描 src/components/ → src/app/ → src/lib/ 再生成
6. 語意與 SEO：H1 唯一，圖片必須有意義 alt
7. 防禦性 UI：無資料 EmptyState、圖片 onerror fallback、流體排版
8. 資安防禦：Supabase key 僅 server-side，絕不暴露前端
9. 內容鎖定：src/data/exam-content.ts 的試卷內容一字不可改
10. 修改紀律：大重構前提醒使用者確認 Git Commit 狀態
11. 視覺驗收：每次完成後啟動 npm run dev，列舉 http://localhost:3000
12. **雙軌並行**：Gemini 內容需在同一輪完成「憲章寫入 → 資料庫寫入 → UI 整合 → 啟動 dev server」，嚴禁分兩輪處理

【字體 SOP（詳見 AGENTS.md SOP 1–3）】
- 大字（≥ 3rem）：letter-spacing ≤ 0.02em，行高 ≥ 1.25
- 中型（≥ 2rem）：letter-spacing ≤ 0.04em，行高 ≥ 1.32
- 中文長句（≥ 18 字當量）：text-wrap: balance

【斷點標準（詳見 AGENTS.md SOP 5）】
- 主斷點：768px / 1024px
- 次斷點：480px / 1200px

【題庫紀律（詳見 AGENTS.md 第六章）】
試卷內容定義於 src/data/exam-content.ts，共 180 張試卷：
- 平面圖 201–206 × A–E（30 張）
- 天花板與立面圖 × 4 視角（120 張）
- 透視圖 207–212 × 甲乙丙（18 張）
- 大樣圖 213–224（12 張）

試卷的 code、focus、notes 欄位為內容鎖定白名單，嚴禁擅自增刪修改。

【Pre-flight Checklist（生成前必跑）】
1. grep -rn "var(--" 確認 CSS 變數覆用
2. grep -rn "font-size:\|letter-spacing:" 確認字體合規
3. grep -rn "@media" 確認響應式不新增斷點
4. ls src/components/ 確認無重複元件
5. 確認新增色彩集中於 globals.css :root
6. 確認無新 hex 色碼
7. 確認 font-size ≤ 6rem
8. 確認 ≥3rem 標題有 text-wrap: balance
